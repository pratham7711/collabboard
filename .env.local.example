# CollabBoard environment variables
#
# For local dev: leave VITE_SOCKET_URL unset — Vite proxies /socket.io to localhost:3001
# Run: npm run server   (in one terminal)
#      npm run dev      (in another terminal)
#
# For production: set this to your Railway (or Render) socket server URL.
# See DEPLOYMENT.md for full instructions.
#
# VITE_SOCKET_URL=https://your-collabboard-server.up.railway.app
